import React from 'react';

function MyAmazingComponent() {
	return (
		<div className="wrapper generic-content-container">
			<h1 className="section-title section-title--blue">This is My Amazing React Component</h1>
			<p>React is ok (I prefer Vue and really don't like the whole JSX idea overall), the sky is blue, grass is green.</p>
		</div>
	);
}

export default MyAmazingComponent;